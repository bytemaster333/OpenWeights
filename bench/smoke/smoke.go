// Package main — 1 MiB upload + pin + range-download + byte-compare smoke
// test against live indexd (Phase 1 SC-3 gate). PLAN 07 fills in body.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "smoke: stub — PLAN 07 provides real implementation")
	os.Exit(2)
}
